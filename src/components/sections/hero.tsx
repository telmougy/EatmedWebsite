// Re-export the expandable hero as the canonical Home `<Hero />`.
// The primitive lives at @/components/ui/hero-button-expandable
// (shadcn-style location); this file is the section wrapper used by
// the locale-aware home page.
export { default as Hero } from '@/components/ui/hero-button-expandable';
